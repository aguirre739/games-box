import React from 'react';
import Carousel from 'react-bootstrap/Carousel';

const JuegosDestacados = (props) => {
    return (
            <Carousel.Item interval={1000}>
                <img
                    className="d-block w-100"
                    src=""
                    alt="First slide"
                />
            </Carousel.Item>
    );
};

export default JuegosDestacados;